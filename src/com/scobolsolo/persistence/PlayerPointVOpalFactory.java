package com.scobolsolo.persistence;
import com.scobolsolo.application.PlayerPointV;

public interface PlayerPointVOpalFactory extends com.opal.OpalFactory<PlayerPointV, PlayerPointVOpal> {
	/* This Opal has neither references (foreign keys) nor single lookups (unique keys), so this class has 
	   no actual content.
	*/
}
